# Use dotnet restore on specific folder

parameters:
  - name: solution-folder-path
    displayName: SLN folder path
    type: string
    default: ./

- script: dotnet restore 
  displayName: Restore dependencies
  workingDirectory: $(parameters['solution-folder-path'])